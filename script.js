function converter() {
    const valor = parseFloat(document.getElementById("valor").value); // Obter o valor inserido
    const opcao = document.getElementById("opcao").value; // Obter a opção selecionada
    let resultado;

    // Taxas de câmbio fixas (para um sistema real, idealmente essas taxas seriam dinâmicas)
    const taxaRealParaDolar = 0.20; // 1 Real = 0,20 Dólar
    const taxaRealParaEuro = 0.18
}